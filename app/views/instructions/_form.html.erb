<%= form_for([@instruction.user, @instruction]) do |f| %>
  <% if @instruction.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@instruction.errors.count, "error") %> prohibited this instruction from being saved:</h2>

      <ul>
      <% @instruction.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<div class="well bs-component">
  <div class="field">
    <%= f.label "Name" %><br>
    <%= f.text_field :name, class: "form-control" %>
  </div>
  <div class="field">
    <%= f.label "Link" %><br>
    <%= f.text_field :link, class: "form-control" %>
  </div>
  <div class="field">
    <%= f.label "Opis" %><br>
    <%= f.text_area :description, class: "form-control" %>
  </div>
  <br />
  <div class = "form-buttons">
    <%= submit_tag("Stwórz instrukcję", class: "btn btn-success") %>
  </div></div>
<% end %>
