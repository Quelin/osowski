<div class="col-lg-12">
<div class = "panel panel-primary">
  <div class="panel-heading">
     <h3 class="panel-title">In order to download the invoice click the name of the invoice or copy link below the name and paste it to your browser:</h3>
    </div>
    <div class="panel-body">
      <br /><div class="row text-center pagination-centered">
      <%= link_to("Back to List", user_services_path(@service.user_id), class: "btn btn-primary glyphicon glyphicon-chevron-left") %>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;
      <%= link_to("Add New Invoice", new_service_invoice_path, class: "btn btn-success glyphicon glyphicon-plus") %>
      <br /> <br /></div>

      <% @invoices.each do |invoice| %>
<div class="row">
  <div class="col-lg-12">
<div class="col-lg-10 col-lg-offset-1">
      
            <h2 id="type-blockquotes"><%= link_to(invoice.name, invoice.link) %>
              &nbsp;&nbsp;
              <%= link_to("", edit_service_invoice_path(invoice.service, invoice), :class => 'action edit', class: "btn btn-warning btn-sm glyphicon glyphicon-pencil") %>
              
              <%= link_to '', [invoice.service, invoice], method: :delete, data: { confirm: 'Are you sure you want to delete this equipment?' }, class: "btn btn-danger btn-sm glyphicon glyphicon-trash"%></h2>
      

      <div class="row">
          <div class="col-lg-10">
            <div class="bs-component">
              <blockquote>
                <p>Direct Link:<br /></p>
                <p><%=invoice.link%></p>
                <p>Description: <br /></p>
                <p><%=invoice.description%></p>
                <small>Created at: <cite><%=invoice.created_at.to_s(:db)%></cite></small>
              </blockquote>
            <div style="display: none;" id="source-button" class="btn btn-primary btn-xs">&lt; &gt;</div></div>
          </div>
        </div></div></div></div>

    <% end %>

    
</div>

</div>
</div>